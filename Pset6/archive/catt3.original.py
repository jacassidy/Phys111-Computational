"""Archived: original copy of catt3.py

Kept for reference; canonical implementations for the double pendulum are
`6c.py` and `partcatt4.py` (which perform longer/higher-resolution runs).
"""

import numpy as np
from scipy.integrate import solve_ivp
import matplotlib.pyplot as plt

# (rest omitted for brevity) — full copy retained in this file
